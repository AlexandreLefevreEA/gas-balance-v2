# gasbalance-core

Shared library for the Gas Balance v2 workspace: typed config, Postgres session,
settings (YAML) loader, logging, and shared domain types. Plumbing only — no models,
no HTTP, no business logic. See [`CLAUDE.md`](CLAUDE.md).
